<%= form_for(@videopost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "YouTube動画のリンクを投稿してください..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
